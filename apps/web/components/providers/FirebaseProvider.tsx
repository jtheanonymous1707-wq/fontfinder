// apps/web/components/providers/FirebaseProvider.tsx
"use client";

import React from "react";

/**
 * Placeholder FirebaseProvider.
 * In a real app, this would initialize Firebase and provide the context.
 */
export function FirebaseProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
